<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head') -%> 
    <body>
        <div class="container-fluid">
            <div class="row">
                <%- include('partials/sidebar') -%> 
                <div class="col-sm-10">
                   <h1><%= title %> </h1>
                   <h4><strong>ID: </strong> <%= bookinstance._id %> </h4>
                   <h4><strong>Imprint: </strong> <%= bookinstance.imprint %> </h4>
                   <p>Are you sure you want to delete? </p>
                   <form action="" method="post">
                        <div class="form-group">
                            <input 
                                type="hidden" 
                                value=<%= bookinstance._id %>
                                class="form-control"
                                name="bookinstanceid"
                                required
                                id="bookinstanceid"
                            >
                        </div>
                    <button class="btn btn-primary" type="submit">Submit</button>
                   </form>
                </div>
            </div>
        </div>
    </body>
</html>